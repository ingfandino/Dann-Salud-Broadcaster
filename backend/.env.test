# Base de datos (no se usa porque mongodb-memory-server genera su propia URI en memoria)
MONGO_URI=mongodb://127.0.0.1:27017/dannsalud_test

# JWT para firmar tokens durante los tests
JWT_SECRET=supersecrettest
JWT_EXPIRES_IN=1h

# Sesiones de WhatsApp simuladas
SESSION_SECRET=testsession

# Entorno de pruebas
NODE_ENV=test

# Evita errores de CORS en tests
ALLOWED_ORIGINS=http://localhost:5173