# ===================================
# CONFIGURACIÓN DE DANN SALUD BROADCASTER
# ===================================

# --- BASE DE DATOS ---
# Para producción, usar credenciales seguras y autenticación
MONGODB_URI=mongodb://username:password@localhost:27017/dann-salud-broadcaster?authSource=admin
# O para MongoDB Atlas (recomendado para producción):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dann-salud-broadcaster?retryWrites=true&w=majority

# --- SERVIDOR ---
PORT=5000
HOST=0.0.0.0
NODE_ENV=production

# --- SEGURIDAD ---
JWT_SECRET=your-secret-key-here-change-in-production
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# --- WHATSAPP ---
# ✅ IMPORTANTE: Activar sistema multi-sesión (RECOMENDADO)
# Permite que múltiples usuarios vinculen WhatsApp simultáneamente
USE_MULTI_SESSION=true

# Ruta base para sesiones de WhatsApp (multi-sesión)
WHATSAPP_SESSION_BASE=./.wwebjs_auth_multi

# Ruta para sesión única (solo si USE_MULTI_SESSION=false)
WHATSAPP_SESSION_PATH=./.wwebjs_auth

# Ruta personalizada de Chrome/Chromium (opcional)
# WHATSAPP_CHROME_PATH=/usr/bin/chromium-browser

# Proxy HTTPS (opcional)
# HTTPS_PROXY=http://proxy.example.com:8080

# --- CONCURRENCIA Y RENDIMIENTO ---
# ✅ Número máximo de jobs concurrentes (recomendado: 2-3)
MAX_CONCURRENT_JOBS=2

# Número máximo de conexiones WhatsApp concurrentes
MAX_CONCURRENT_CONNECTIONS=5

# --- AUTO-RESPUESTAS ---
# Ventana anti-spam en minutos
AUTORESPONSE_WINDOW_MINUTES=30

# --- RECUPERACIÓN Y REINTENTOS ---
# Máximo de intentos para reiniciar sesión de WhatsApp
WA_RESET_MAX_ATTEMPTS=2

# --- ARCHIVOS ESTÁTICOS ---
# Ruta del build del frontend (producción)
# FRONTEND_BUILD_PATH=/path/to/frontend/dist

# Proteger carpeta uploads con autenticación
PROTECT_UPLOADS=true

# --- EMAIL (para recuperación de contraseñas) ---
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# EMAIL_FROM=noreply@dann-salud.com

# --- LOGS ---
# Nivel de logs: error, warn, info, debug
LOG_LEVEL=info
